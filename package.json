{
  "name": "finance-attendance",
  "version": "1.0.0",
  "scripts": {
    "install-all": "npm install --prefix frontend && npm install --prefix backend",
    "build": "npm install && npm run install-all && npm run build --prefix frontend",
    "start": "node backend/index.js",
    "dev": "npx concurrently \"npm run dev --prefix frontend\" \"node backend/index.js\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
